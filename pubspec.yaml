name: searah_backend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.0
  path: ^1.9.0
  http: ^1.1.0
  shared_preferences: ^2.5.3
  google_sign_in: ^6.1.5
  provider: ^6.0.5
  flutter_map: ^8.2.2 # atau versi terbaru
  latlong2: ^0.9.1 # atau versi terbaru
  cached_network_image: ^3.3.0
  geocoding: ^2.1.1
  geolocator: ^13.0.1
  flutter_map_tile_caching: ^10.1.1
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.0.0
  quickalert: ^1.1.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
